"""agentgrid: a local dashboard over every Claude Code (and Codex) session."""

# The one place the release number lives. `ag --version` prints it, with the
# checkout's commit beside it when run from a git clone -- a clone is the
# install, so the commit is what actually identifies the code being run.
__version__ = "0.1.0"
